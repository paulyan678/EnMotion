# 更新日志

EnMotion 的重要改动都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/)。

---

## [1.0.18] - 2026-08-03

### 新增

- **文本生成请求统一审核** — 提取实体、分析剧本和生成分镜现在使用同一个请求审核器；用户可在
  提交前选择实际模型、编辑完整指令与源文本，并检查锁定的输出契约和最终服务商请求。
- **工作区导航支持收起** — 系列与单集工作区的顶部导航可向上收起，并提供紧凑的恢复入口；
  收起状态会在当前桌面会话中保持。

### 优化

- **创作步骤释放更多内容空间** — 资产库、脚本、艺术指导和分镜页面移除重复标题栏；提取实体、
  查看脚本和生成分镜等操作会随所属内容滚动离开，不再长期占用可视区域。
- **账户信息集中到首页侧栏** — 额度与用户入口集中放入首页左侧导航，首页顶部空栏和工作区内
  重复账户信息已移除，移动端设置页仍保留必要入口。
- **请求记录与实际调用一致** — 文本任务冻结已审核的模型、指令、源文本、输出契约和校验和，
  并把最终发送内容写入生成记录，避免界面审核内容与服务商调用漂移。

### 修复

- **艺术指导详情不再被侧栏遮挡** — 风格详情、AI 推荐与确认弹窗统一通过顶层门户显示，并补齐
  对话框语义、键盘焦点和可见焦点状态。
- **文本调用避免重复提交** — 服务端强制校验审核校验和，并在短时窗口内按脚本和请求身份复用
  已完成结果，降低响应丢失后重复扣费或重复生成的风险。
- **分镜前置检查跳转正确** — 缺少脚本时会准确返回脚本步骤，不再触发无效的内部导航事件。

### 验证

- 通过后端 969 项、前端 253 项逻辑与 323 项界面测试，以及类型检查、Lint、生产构建、静态导出、
  配色令牌和体积预算门禁。
- 使用真实 EnMotion 界面验证顶部导航收起/恢复、滚动操作、三类文本请求审核和艺术指导顶层弹窗；
  测试数据已清理，未提交任何文本、图片或视频服务商生成请求。

## [1.0.17] - 2026-08-02

### 优化

- **设置页回归单层结构** — 移除“通用”等二级分类栏及主题、动效、更新和接口配置中的灰色
  解释文案；主题、动效开关、模型访问设置和更新操作直接排列，减少无效层级与滚动距离。
- **资产收藏按钮统一尺寸** — 资产卡片上的收藏图标改为紧凑的 32 像素视觉尺寸，同时保留
  44 像素可点击区域、键盘语义和加载状态，不再遮挡资产缩略图。
- **创作步骤显示更多实际内容** — 动态与合成页面移除重复的“动效生成”和“时间线组装”
  标题栏，分镜首帧、动态参数和时间线内容可直接使用释放出的垂直空间。
- **资产编辑器去除重复归属提示** — 角色、场景和道具编辑器不再显示“系列资产 · … · 影响
  若干集”等重复标签；资产类型、保存状态、提示词与生成设置保持清晰可见。

### 修复

- **资产详情支持点击外部关闭** — 资产库右侧详情面板打开后，点击面板以外任意位置即可关闭；
  面板内的收藏、编辑、下载和删除操作不会误触关闭。

### 验证

- 通过前端 250 项逻辑与 321 项界面测试、类型检查、Lint、生产构建和桌面配置门禁。
- 使用本地临时资产、系列、单集及文字分镜在实际 EnMotion 界面验证设置、资产库、资产编辑、
  动态和合成流程；浏览器控制台零错误，测试数据已清理，未提交任何图片或视频生成请求。

## [1.0.16] - 2026-08-02

### 新增

- **系列与单集信息统一编辑** — 系列名称、系列简介、单集名称、单集简介和剧本概要现在都可在
  系列页、项目卡片和工作区内直接编辑；修改会写回同一份持久化数据并在所有入口同步显示。
- **模型访问权限检查** — 管理中心新增不计费的模型访问检查，可验证每个已配置模型是否可用并
  记录最近检查时间；界面明确区分 EnMotion 点数、模型访问权限与服务商现金余额。
- **润色模型改为逐次选择** — 分镜与动态提示词润色在每次调用旁直接选择聊天模型，选择会随
  本次请求明确发送而不会保存成系列或单集的隐藏默认值。
- **隔离的真实应用测试档案** — 打包应用可使用受严格约束的临时测试档案运行，测试数据、日志
  和输出全部保存在系统私有应用数据目录下，并可通过带清单校验的安全命令完整清理。

### 优化

- **信息概览改为真实数据** — 系列与单集概览会解析单集、系列和全局资产继承关系，准确统计
  角色、场景、分镜及完成片段，不再出现有内容却显示为零的占位统计。
- **生成请求内容更易读** — 请求审核以模型、生成方式、完整提示词、参考素材和参数为主体；
  编译器标识、校验和、原始模型编号与文件路径收纳到折叠的技术详情，不再干扰创作流程。
- **创作入口去除重复默认项** — 设置页、系列页和单集页不再提供会相互覆盖的默认创作模型；
  用户在实际生成时选择模型、检查并编辑最终提示词，模板也只修改提示词内容。
- **面向用户的文案去内部实现化** — “新接口”、后端、工作进程、轮询及原始模型编号等内部
  实现词从普通界面移除；模型说明、生成记录和失败信息改为用户可理解的功能与处理建议。

### 修复

- **Seedance 请求在保存前严格验证** — 工作区和创作台对三个 Seedance 模型的文生视频与
  图生视频统一验证精确模型、模式、首帧、时长、分辨率、比例、随机种子、音频和水印类型；
  无效或被篡改的审核请求不会创建任务，也不会产生服务商调用。
- **生成输出持续免磁盘权限** — macOS 输出与测试档案继续固定在应用私有数据目录；桌面验证
  会阻止文稿目录、Full Disk Access 描述或任何越出应用数据目录的默认路径回归。
- **创作台操作区不再被面板遮挡** — 参数和请求审核区域保持在可滚动内容区，生成操作栏独立
  参与布局；增加响应式回归测试，防止浮层、固定栏或模型面板覆盖按钮。
- **分镜类型会真实影响请求** — 跟拍、推镜等运镜选择会作为可见、可审核的构图指令进入最终
  图片和视频提示词，不再只是界面分类；重复生成时也不会反复叠加同一条指令。
- **资产构图比例保持一致** — 全身图等素材即使沿用参考图存储结构，也会按用户当前选择的
  宽高比编译请求，不再被内部素材类型强制改成方图。
- **文本调用进入统一生成记录** — 实体提取、视觉风格、分镜分析与润色、视频提示词润色和
  文件分集等聊天模型请求都会记录实际模型、输入、来源、耗时和成功/失败状态，不再出现
  点数已扣除但生成记录为空的情况。

### 验证

- 通过后端 965 项、控制服务 78 项、前端 249 项逻辑与 320 项界面测试、桌面 Python 39 项
  及 Rust 14 项测试；通过类型检查、Lint、静态导出、体积预算、模型目录和桌面配置门禁。
- 使用隔离的 macOS 打包应用按真实用户流程完成 17 项生成请求：GPT Image 2，以及三个
  Seedance 模型在创作台、工作区和资产库中的文生视频与图生视频路径全部成功，零失败。

## [1.0.15] - 2026-08-02

### 新增

- **统一生成请求审核** — 创作台、工作区素材、分镜图片、动态片段与全局资产库现在都在
  提交前展示准确服务商请求，包括完整提示词、实际模型、输入素材、参数、生成模式、编译器
  版本与稳定校验和；修改任一输入后必须重新审核，避免界面显示内容与实际请求漂移。
- **不可变任务溯源** — 每个图像、视频、素材与分镜任务都会冻结本次编译请求，接口调用详情
  可回看当时真正发送的内容；重试继续使用原始快照，不会被之后的项目设置或提示词修改污染。

### 优化

- **生成时直接选择模型与编辑提示词** — 移除设置、系列和项目中重复的默认提示词与默认生成
  模型入口；模型与完整提示词集中在实际生成位置，素材库原有的年龄、氛围等分散字段合并为
  可自由编辑的统一提示词，减少互相覆盖的隐式配置。
- **精确素材与模式契约** — 图生图、图生视频、文生视频和分镜任务只发送用户在审核页看到的
  精确素材；文生视频不再携带旧首帧，图生视频强制且仅使用当前选中的一张首帧。

### 修复

- **请求审核面板不再挤压或遮挡操作** — 共享审核面板保持完整高度并参与正常布局，创作台和
  工作流中的生成按钮在所有模型、参数展开状态和可滚动区域中都可见、可点击。
- **失败信息保持服务商原意** — 认证、权限、额度、并发和政策拒绝继续按永久或瞬时类别展示；
  永久的 401/403 不会自动重复计费重试，接口调用记录保留有界诊断与请求快照。
- **创作台溯源完整显示** — 混合桌面模式将创作台历史中的冻结编译请求原样映射到接口调用
  详情，完整提示词、模型、参数、参考素材与校验和不再退化成旧版摘要。

### 验证

- 通过后端 933 项、控制服务 77 项、前端 248 项逻辑与 318 项界面测试、桌面 Python 36 项
  及 Rust 13 项测试，并通过类型检查、Lint、静态导出、体积预算、模型目录与桌面配置门禁。
- 使用实际 EnMotion 界面完成创作台、工作区与资产库的文本、文生图、图生图、文生视频和
  图生视频调用；Seedance 2.0 Fast 与 Mini 的 T2V/I2V 均成功。当前服务商提供的 Standard
  凭证返回 HTTP 401，Fast/Mini 凭证访问 Standard 返回 HTTP 403，EnMotion 会准确显示此
  外部权限阻塞而不会盲目重试或暗中替换模型。

## [1.0.14] - 2026-08-01

### 新增

- **工作区文生视频模式** — 动态步骤的每个分镜现在都可在“首帧驱动（图生视频）”与
  “提示词驱动（文生视频）”之间切换；所选模式按镜头持久化，任务结果明确标注 T2V/I2V，
  三个 Seedance 模型都使用相同的镜头级参数和队列体验。

### 修复

- **服务端严格区分视频输入** — 工作区 T2V 仍由服务端校验所属分镜和规范运镜，但拒绝
  任何图片输入；I2V 继续强制验证精确的分镜图片标识、URL 与当前首帧选择，避免模式切换
  后误传旧图片或绕过分镜归属校验。
- **模式感知的提示词与重混** — T2V 提示词润色不再附带图片，重混任务会恢复原始生成
  模式，且模式保存失败时界面会回滚并显示可操作错误。
- **生成按钮不再被提示词自动保存吞掉** — 点击“生成片段”前由失焦触发的提示词保存现在会被
  生成流程复用并等待完成，而不会在点击瞬间短暂禁用按钮；快速编辑后立即生成也会可靠提交。
- **创作台面板不再遮挡生成按钮** — 参数卡片与生成操作现在使用独立布局行；桌面端只滚动
  表单区域，移动端保持自然页面滚动，任何模型或参数面板都不会覆盖生成按钮及其点击区域。
- **低余额不再误报模型权限** — 服务商以 HTTP 403 返回低余额保护阈值时，EnMotion 现在
  正确显示额度不足并保留退款记录，而不会错误提示所选模型无权使用。

### 验证

- 新增工作区 T2V 无图片输入、I2V 精确图片校验、模式持久化及前端任务提交回归覆盖；
  发布门禁包含创作台与工作区中三个 Seedance 模型的 T2V/I2V 实际界面矩阵。

## [1.0.13] - 2026-08-01

### 优化

- **生成状态请求收敛** — 长任务等待改为按任务标识查询并随任务时长自适应降频；
  分镜批量状态合并为单次请求，同一项目的多个生成界面共享一个可见性感知、无重叠的
  项目观察器，减少重复全量项目与活动历史传输。
- **失败诊断保持完整** — 等待器抛出的类型化终态错误保留任务标识、错误码和有界诊断，
  失败不会再被压缩成无法追踪的通用消息；新增专用活动查询和批量状态契约及回归测试。
- **服务商重试不再成倍放大** — 控制服务会明确标记已耗尽的上游重试，桌面端及聊天适配器
  不再用新幂等键重复整轮提交；未耗尽的明确退款请求仍保留安全的有界恢复路径。
- **错误服务商配置无法上线** — 管理员保存共享 API 配置前会使用非计费的模型列表接口，
  并行验证 TLS、端点可达性、每个密钥及其模型访问权限；瞬时故障有界重试，失败配置不会
  写入或替换当前可用版本。
- **前端与素材体积预算** — 15 张内置风格缩略图从 29.88 MiB PNG 优化为
  1.35 MiB WebP，静态前端导出从约 38 MiB 降至 4.65 MiB；CI 与发布流程会阻止
  素材、总导出、JavaScript、最大分块或 CSS 超出预算。
- **macOS 运行包瘦身** — 启动 sidecar 从约 167 MiB 降至 122 MiB，按需 Demucs
  工作器从约 168 MiB 降至 124 MiB；桌面包不再携带服务端数据库、开发工具和模型
  未使用的编译/展示依赖。
- **真实音频发布门禁** — 修复冻结 Demucs 工作器的 NumPy 兼容模块与多进程资源跟踪器
  重入问题；发布构建现在必须完成一次真实双声道人声分离并产出两个音轨，不能只靠导入
  检查通过。

### 验证

- 通过后端 918 项测试（70.13% 覆盖率）、控制服务 76 项测试（76.07% 覆盖率）、
  前端 562 项逻辑与 UI 测试、桌面 Python 36 项及 Rust 13 项测试；通过类型、格式、
  静态导出、数据库迁移漂移、Python/npm 依赖安全审计和冻结 sidecar 验证。

## [1.0.12] - 2026-08-01

### 修复

- **图像、视频与文本任务可靠派发** — 桌面混合模式将创作台与工作区生成
  改为脱离 HTTP 请求生命周期的受管工作线程，并显式传递用户与工作区租户上下文；
  前端点击后立即持久化任务并在接口调用面板显示真实状态。
- **已接收视频任务安全恢复** — 服务商任务标识在提交后立即原子持久化；桌面重启、
  状态轮询中断或用户重试时继续查询同一个已付费任务，不会重复提交。所有视频模型
  共享服务商并发队列，在 EnMotion 内串行等待，并对服务商显式并发限制退款后重试。
- **批量生成不再丢失或重复结果** — 首个失败会保留已完成输出、显示可重试的部分完成状态，
  再从缺失索引继续；不再将数量不足的批次标记为成功。
- **可诊断且可控的服务商失败** — 控制服务传递并发限制、输入隐私、输出政策、配额、
  权限和不确定结果等类型化错误；仅在服务商未接收时自动重试，对成功图像响应使用
  加密、有界且可过期的 24 小时精确重放缓存，避免网络中断后重复计费。
- **长任务会话与素材并发写入恢复** — 长时间图像请求遇到员工会话过期时刷新会话并以
  同一幂等键恢复；素材库在生成期间保存提示词或元数据时保留规范素材对象，不再让已完成
  媒体落到过期副本；接口调用页面首次打开会立即加载真实任务，而不是停留在空白状态。
- **文本与提示词调用契约** — 纯文本对话不再附带空图片输入；提示词润色使用严格 JSON
  契约，并将模型原样返回降级为可见警告；托管聊天只会在明确退款且服务商未接收时重试。
- **macOS 输出永久免磁盘权限** — 所有后续版本的本地生成媒体固定写入
  `~/Library/Application Support/com.enmotion.desktop/enmotion-output`，不再使用“文稿”目录，
  发布验证会阻止该路径或多余磁盘权限回归。

### 验证

- 通过后端 913 项测试（70.05% 覆盖率）、控制服务 71 项测试（75.69% 覆盖率）、
  前端 560 项逻辑与 UI 测试、桌面 Python 35 项及 Rust 13 项测试，并通过类型检查、
  格式、安全审计、数据库迁移漂移和生产构建检查。
- 使用实际打包的 macOS 应用完成真实 UI 压力验证：在创作台、工作区和素材库运行文本、
  文生图、图生图、文生视频和图生视频；覆盖 3 个聊天模型、GPT Image 2 以及 Standard、
  Fast、Mini 三个视频模型，并验证页面切换、并行派发、批量续传和生成中元数据保存。

## [1.0.11] - 2026-08-01

### 修复

- **macOS 测试包恢复登录与创作服务** — 混合桌面共用的服务商媒体路径改为直接使用
  服务端中立的工作区路径模块，避免间接加载仅服务端需要的 SQLAlchemy；冻结 sidecar
  现在会在打包阶段真正初始化混合模式核心应用，缺失运行时依赖会在发布前直接失败。

### 验证

- 新增混合媒体模块依赖边界和冻结核心初始化契约测试，并使用实际冻结 sidecar 验证
  登录、工作区读取及创作台任务派发。

## [1.0.10] - 2026-08-01

### 修复

- **创作台任务立即派发** — 创作台生成记录使用独立的跨进程锁，并在混合桌面模式中
  通过独立的任务接收路径持久化；即使分镜或其他项目生成正在等待外部 AI 服务商，
  新任务也会立即获得任务标识并进入状态轮询，不再长时间停留在“派发中”。登录、
  CSRF 校验、工作区隔离和项目写入锁保持不变，服务商连接失败仍会作为明确的终态错误返回。

### 验证

- 新增全局工作区写入锁占用期间的创作台存储和 HTTP 派发并发回归测试，并运行创作台、
  混合桌面、持久化、工作区隔离及生成状态轮询测试。

---

## [1.0.9] - 2026-07-31

### 修复

- **生成结果与 API 记录一致** — 图像、素材、分镜与视频任务从提交到终态使用同一
  任务标识和明确的规范所有者；只有找到本次请求新增的持久化媒体后才记录成功，
  不再通过历史素材猜测或补写记录。API 调用面板只展示真实生成生命周期，图片缩略图
  使用已认证媒体路径，并移除重复的账务记录卡片。
- **生成期间界面保持可用** — 创作台、素材编辑器、分镜和 API 调用面板统一使用可取消、
  串行的任务轮询；页面切换会终止所属轮询，不会叠加定时请求、永久加载或导致桌面黑屏。
  新生成的分镜图同时写入当前选图、历史图和视频首帧字段，后续步骤立即读取同一结果。
- **桌面登录不再调用钥匙串** — 可撤销刷新令牌改为原子写入 EnMotion 应用数据目录中的
  所有者专属文件，macOS/Linux 使用 `0700` 目录和 `0600` 文件权限；不保存账号密码，
  不触发系统钥匙串授权窗口，读写错误会直接阻止不完整的会话状态。
- **首页布局与操作精简** — 工作区、资产库、创作台和接口调用使用同一标题组件与间距；
  创作台和接口调用的分隔线已移除，项目顶部无效钥匙按钮已移除，素材编辑器文字和侧栏
  保持在各自容器内。
- **错误与依赖安全** — 图像服务连接失败、视频输入隐私拒绝和输出政策拒绝均返回明确、
  可操作且不泄露供应商细节的错误；升级 `cryptography` 和控制服务测试工具到已修复版本。
- **macOS 测试包可独立启动** — 工作区路径解析移到桌面与服务端共享的轻量模块，桌面
  sidecar 不再因只属于服务端数据库的 SQLAlchemy 导入而在打包验证或启动时失败。

### 验证

- 运行完整 Python、控制服务、前端、桌面契约与 Rust 测试，覆盖 API 活动、规范素材所有者、
  可取消轮询、图像渲染、会话文件权限和错误分类；通过格式、类型、静态导出、依赖漏洞、
  原生打包配置和仓库卫生检查，并以实际账号完成生成中跨页面、素材编辑和图片结果渲染冒烟。

---

## [1.0.8] - 2026-07-30

### 修复

- **API 请求详情浮层** — 请求详情通过共享顶层 Portal 渲染并使用统一焦点管理，
  不再受页面祖先层叠上下文影响，标题、状态与关闭按钮不会被顶部账号栏遮挡。
- **图像生成错误准确性** — 图像生成与图像编辑请求使用正确的图像阶段诊断；
  批量素材生成在全部尝试失败时保留计费规则等可操作供应商错误，不再错误标记为
  视频提交或降级成笼统失败。

### 验证

- 新增请求详情 Portal 层级、图像计费错误阶段及批量素材错误传播回归测试，并运行
  完整 Python、控制服务、前端、桌面契约、Rust、构建、安全与仓库卫生检查。

---

## [1.0.7] - 2026-07-30

### 修复

- **生成期间保持界面可用** — 混合桌面模式的读取请求改为使用不可变工作区快照，
  任务轮询直接读取当前生成状态，避免长时间图像或视频请求占用写锁时让素材编辑器、
  API 调用面板及其他只读界面持续加载。API 活动来源均设置有界超时，异常时会显示
  可重试错误而不是无限等待。
- **素材生成记录完整性** — 本地工作区、系列与全局素材生成现在会写入独立、原子且
  按工作区隔离的活动记录，包含素材名称、提示词、模型、来源和完整生命周期；应用
  意外重启时会把未完成记录标记为失败，避免 API 调用面板缺少或永久显示运行中的任务。
- **计费规则确定性与删除** — 管理中心明确显示优先级和版本，同一操作、模型与筛选
  条件下按最高优先级、再按最新版本选择生效规则。新增安全删除操作，以软删除方式
  停用后续请求，同时保留既有用量、额度账本和审计记录。缺少计费规则时，生成界面
  返回可操作的中文说明，不再只显示笼统失败。
- **素材编辑器布局** — 编辑器和加载/错误状态通过顶层 Portal 渲染，不再被系列步骤
  侧栏或祖先容器遮挡；艺术指导说明在窄面板内正确收缩和截断，不会溢出卡片边界。
- **界面精简与一致性** — API 调用筛选器采用与工作区一致的分段标签样式，并移除实时
  更新、更新时间、刷新按钮和说明文字。创作台移除副标题与结果计数。全屏图片预览
  移除“复制地址”和“在新标签打开”，只保留关闭与必要的分组导航。

### 验证

- 新增工作区写锁并发读取、任务状态轮询、本地活动持久化与重启恢复、计费规则优先级、
  软删除历史完整性、供应商错误分类、素材编辑器 Portal、文本精简和预览操作的回归测试。
- 运行完整 Python、控制服务、前端、桌面契约与 Rust 测试，以及覆盖率、格式、类型、
  静态导出、数据库迁移漂移、依赖漏洞和仓库卫生检查。

---

## [1.0.6] - 2026-07-30

### 修复

- **系列生成模型一致性** — 实体提取、分镜、艺术分析、提示词润色和上一集概要
  均使用系列实际配置的模型；实体预览与应用使用一次性修订令牌，避免过期内容、
  跨项目内容或重复请求被错误写入。项目与系列设置可逐项继承或重置，不会因
  打开并保存设置窗口而把当前默认值静默固定为覆盖项。
- **脚本与工作区持久化** — 脚本失焦保存按项目排队、合并并支持失败重试；
  切换项目不会把旧草稿写入新项目。手动同步会刷新系列和项目，并清理已删除的
  远端实体，不再保留失效的本地列表。保存失败的草稿带有持久化脏标记，离开
  页面或重新加载后仍会恢复并重试，不会被本地乐观状态误认成服务器已保存。
- **创作台输入与资产保存** — 图像与视频模式仅发送各自支持的参数和参考媒体；
  资产保存必须明确选择角色、场景或道具，失败时显示本地化提示。下载统一使用
  已认证请求，并拒绝错误响应或空文件。缺少必需参考图时禁止提交，调度失败会
  显示可操作错误；不受供应商支持的负向提示词和视频参考操作不再显示为假功能。
- **API 调用记录准确性** — 实际生成任务与计费结算记录分开呈现；失败、取消、
  退款和结算状态不再被误标为成功生成，并在列表与详情中显示真实结算结果；
  任一记录来源加载失败时不再把不完整列表伪装成成功结果。
- **配音、混音与导出** — 配音预览只应用于当前选中的镜头版本；项目切换或旧的
  异步响应不会串用导出文件。不可用的背景音乐和未实现的音效电平会明确禁用，
  同时可上传项目专属的本地音频作为背景音乐；替换、清除、失败回滚和跨项目路径
  均安全处理。已选背景音乐混音失败时导出会报告错误而不是静默生成无声结果。
  镜头、版本、配音、顺序或混音发生变化时会以事务方式失效并清理旧合成文件；
  合成、导出及相关编辑按项目互斥，避免 FFmpeg 读取过程中混入新旧状态。
- **桌面生命周期与媒体访问** — macOS 关闭窗口后可通过 Dock 或再次启动恢复；
  sidecar 文件读取需要有效会话并支持安全的流式 Range/HEAD 响应。混合模式配置
  会拒绝受保护的环境字段，任务轮询按本地或托管来源使用正确端点。系统凭据库
  授权窗口被遮挡时不再让工作区无限加载；凭据读取保持有界且单飞，序列化写入
  不再被无法中断的读取饿死。macOS 会在启动 sidecar 前主动请求“文稿”文件夹
  权限，拒绝后提供准确的系统设置路径，而不是等到工作区操作时静默卡住。正式
  发布的核心 sidecar 使用跨版本稳定的签名标识，避免重复触发旧钥匙串 ACL。
- **界面一致性与可访问性** — 修复浮层遮挡、安全区域、浅色主题对比度、时间戳
  时区、视图切换标签、更新失败指引、无脚本旧数据兼容和未就绪状态说明。系列
  创建与工作区同步使用有界请求；超时后恢复按钮、保留输入并提示先同步确认，
  避免用户因未知结果重复创建系列。

### 验证

- 覆盖系列模型路由、实体修订令牌、脚本跨项目保存、Playground 模式约束、资产
  分类、计费状态、配音/混音/导出、sidecar 鉴权、UTC 时间戳和桌面窗口恢复的
  回归测试；补充被阻塞的钥匙串读取、macOS 输出目录访问探针和工作区快速请求
  超时恢复测试。
- 运行完整 Python、控制服务、前端、桌面契约与 Rust 测试，以及格式、类型、
  静态导出、迁移漂移、安全审计和原生桌面构建检查。
- 持续集成与桌面发布流程改用固定提交的 Node 24 版 checkout、setup-node
  与 setup-python Actions，移除已停用 Node 20 运行时的兼容告警；签名任务禁用
  隐式包缓存，并支持 DigiCert 官方 RFC 3161 时间戳服务。应用构建工具链同步
  升级至仍受安全维护的 Node.js 24.18 LTS 与 npm 11.16；依赖安装脚本采用
  严格、逐版本审核的许可清单，开发工具链使用已修复内存耗尽问题的
  `brace-expansion` 兼容维护版本。控制服务测试同时清除 HTTP 客户端、
  422 状态常量与迁移配置的弃用告警。

---

## [1.0.5] - 2026-07-29

### 修复

- **管理服务连接韧性** — 桌面端在代理隧道建立、TCP 连接或 TLS 握手出现短暂
  故障时，以受限次数和指数退避自动重试，减少网络线路抖动导致的登录失败。
- **失败边界保持明确** — 身份验证失败与控制服务返回的 HTTP 错误不会被误判为
  连接抖动，也不会触发重复登录重试。

### 验证

- 新增代理连接与 TLS 握手连续失败后恢复的回归测试，并验证连接超时上限与退避
  序列。
- 在 Apple 芯片 Mac 上重新打包原生 sidecar 与 Tauri 应用，并确认发布构建携带
  生产管理服务端点。

---

## [1.0.4] - 2026-07-27

### 变更

- **管理中心品牌横幅** — 移除重复的“企业管理控制台”顶栏文字，重新对齐
  EnMotion 标志与管理中心标题，并按照官方品牌规范以青绿色突出显示
  `Motion`。
- **响应式布局** — 品牌名称在窄屏保持完整，并允许“管理中心”标签在空间不足
  时自然换行，避免与登录状态和退出操作重叠。

### 验证

- 新增管理中心静态回归断言，并在桌面与窄屏视口检查品牌颜色、对齐和换行。

---

## [1.0.3] - 2026-07-27

### 变更

- **管理员重置密码** — 管理中心与桌面账号管理界面现在会直接显示正在输入的
  新密码，并允许管理员使用至少 6 个字符的临时密码。
- **策略范围保持不变** — 创建账号与用户自行修改密码仍要求至少 12 个字符；
  管理员重置后仍会撤销该账号的全部现有登录会话并保留审计记录。

### 验证

- 新增 5/6 字符边界、纯空格拒绝、旧密码与会话失效、短密码重新哈希、
  明文输入显示及成功后清除的回归测试。

---

## [1.0.2] - 2026-07-27

### 变更

- **管理服务域名迁移** — 新版桌面应用连接
  `https://enmotion.tianen123.xyz:9443`，不再将安装包绑定到服务器 IP
  派生域名。
- **旧版平滑升级** — 控制服务仅对明确允许的新旧 HTTPS 来源分别签发同源
  更新地址，使仍连接旧域名的受信任客户端能够安全升级到新版。

### 验证

- 新增更新来源白名单、恶意 Host 回退与配置拒绝测试。
- 发布仍要求完整 CI、macOS 签名与公证、Windows Authenticode、SBOM、
  SHA-256 校验和构建来源证明。

---

## [1.0.1] - 2026-07-26

### 变更

- **官方品牌标识** — 启用由故事画格、字母 E 与播放箭头组成的新 EnMotion
  标志，并统一应用于应用内品牌区、登录页、管理中心与项目文档。
- **原生应用图标** — macOS Dock/Finder、Windows 应用与安装器、网页
  favicon 由同一份规范 SVG 生成，避免平台间视觉漂移。
- **稳定字标** — 官方横向字标转换为 SVG 轮廓，不依赖用户电脑安装的字体。

### 验证

- 新增品牌资产契约检查与前端主题映射测试。
- 发布前仍要求完整 CI、macOS 签名与公证、Windows Authenticode、SBOM、
  SHA-256 校验和构建来源证明。

---

## [1.0.0] - 2026-07-24

### 新增

- **EnMotion 桌面应用** — 提供 Apple 芯片 Mac、Intel Mac 与 Windows x64
  原生安装包；员工无需 Git 或开发环境即可安装。
- **本地优先工作区** — 项目和生成媒体保存在员工电脑，默认输出目录为
  `Documents/enmotion-output`。
- **公司账户与额度控制** — 登录、账户状态、费率、额度、账本和 Provider
  凭证由轻量控制服务统一管理。
- **平滑更新** — 应用内检查、后台下载、签名验证、安全落盘与重启流程不会
  删除登录、设置、项目或生成媒体。

### 发布与安全

- macOS arm64、macOS x64 与 Windows x64 从同一个不可变
  `desktop-v1.0.0` 标签构建。
- 首次安装包通过公开的 EnMotion GitHub Release 页面下载；更新清单只引用
  对应版本的公开 Release 资产，不使用可变的 `latest` 地址。
- 发布包含 SHA-256 校验、CycloneDX SBOM、Tauri Minisign 签名和 GitHub
  构建来源证明。macOS 与 Windows 安装包必须通过各自的平台签名检查。

---

## 发布前功能演进

以下记录描述 EnMotion 1.0.0 定版前的功能开发快照，不代表已发布的
EnMotion 版本号。

### 2026-06-09

#### 新增
- **Qwen 3.7 Plus 支持** — LLM fallback chain 首选升级为 qwen3.7-plus，提示词润色配置新增 3.7 选项（保留 3.6-plus/flash 兼容）

#### 变更
- **主视觉焕新** — Logo 与 Banner 从霓虹莲花渐变风格升级为 Cyber Brutalism 棱角几何风格（白色棱角莲花 + 蓝色水晶核心 + 电路纹理），品牌字标改为 monospace 等宽字体
- **侧边栏品牌区** — 去掉渐变文字，改为 monospace “EnMotion” 品牌字标，Logo 使用无文字版几何标志
- **默认 LLM 模型** — LLMAdapter/QwenVL 默认模型从 qwen3.6-plus 升级为 qwen3.7-plus

#### 修复
- **GPT-Image-2 edit 模式参数** — `--images` 改为 JSON 数组格式传递，修复参考图生成报错

---

### 2026-06-08

#### 新增
- **Playground 创作台** — 全新独立生成模块，无需创建项目即可使用所有图像/视频生成能力
  - 6 种生成模式：图像（T2I + I2I 自动识别）、文生视频、图生视频、参考生视频、视频编辑
  - 两级模式选择器：图像生成 / 视频生成大类切换 + 视频子模式 pill
  - 模型按 family 分组排序（视频: HappyHorse → Seedance → Kling → PixVerse → Wan → Vidu）
  - 每个模型动态参数（GPT-Image-2: size+quality; Kling: mode+sound+cfgScale; Vidu: movementAmplitude+audio 等）
  - 并发任务队列：可连续提交多个生成任务，右侧画廊实时显示状态
  - 网格/画廊视图切换 + 详情面板（左图右信息，←→ 导航）
  - Prompt 模板管理（新建/套用/收藏/删除）+ Prompt 历史（去重/搜索/一键复制/存为模板）
  - 失败任务：重试 + 删除 + 复制报错全文
  - 资产库双向打通：收藏到资产库（toggle）/ 从资产库选取作为输入
  - 批量生成（抽卡 ×1/×2/×4）
  - Session 时间分割线（30 分钟间隔自动分组）
- **GlobalSidebar 创作台入口** — 侧边栏第 4 个导航项（Sparkles 图标）
- **MuleRun 一键登录** — 设置页一键触发 OAuth 登录 + 重新登录按钮
- **GPT-Image-2 扩展尺寸** — 支持 2K (2048×2048) 和 4K (3840×2160) via MuleRun

#### 变更
- **Model catalog 参数精确化** — 所有 27 个 active 模型逐一声明 seed/negativePrompt/promptExtend/watermark 的 true/false，前端按模型动态显示高级参数
- **WanxModel prompt_extend/watermark** — 改为 kwargs 优先读取（修复 Playground 传参被忽略的问题）
- **PixVerse 路由** — Playground service 改为走 WanxModel 通道（与 pipeline 对齐）
- **图像参数体系** — 图像模式显示 size（如 1024×1024 (1:1)），视频模式显示 resolution/ratio，不再混用

#### 停用
- **Wan 2.6 全系列全局隐藏** — wan2.6-i2v、wan2.6-i2v-flash visible_in 清空 + wan2.6-r2v 标记 deprecated，Studio 和 Playground 统一不再展示
- **Wan 2.5 / 2.2 系列** — 确认全部 deprecated + visible_in=[]

#### 修复
- **Vidu watermark 误标** — catalog 从 true 改为 false（代码中无此参数）
- **收藏状态不同步** — 改为从 store generation 数据驱动（单一数据源），卡片/详情面板自动一致
- **下载打开新标签** — 改为 fetch→blob→createObjectURL 强制浏览器下载
- **筛选不显示失败任务** — 改为按 mode 判断分类，不依赖 outputs

---

### 2026-06-05

#### 新增
- **MuleRun/MuleRouter provider** — 通过 MuleRun 平台调用 Seedance 2.0 (T2V/I2V/R2V) 和 GPT-Image-2 (T2I/I2I)，一个账号统一计费
- **MuleRun CLI 双模式** — 支持 CLI subprocess 模式（`mulerun login` 登录）和 HTTP API 模式（`MULEROUTER_API_KEY`），自动检测优先级
- **R2V 模型一等公民** — 独立 `selection_group: r2v`，8 个 R2V 模型跨 6 个 family 直接可见可选，消除旧的 hidden + 推导架构
- **reference_sheet 生成类型** — R2V 角色设定图一次 T2I 生成（含特写 + 三视图），替代旧的 full_body → three_view → headshot 三步流水线
- **GroupedModelGrid 组件** — 模型按 family 分组展示（带 display_name 标题），覆盖 6 个设置/选择组件
- **Family display_name** — Catalog YAML 支持 `display_name` 字段，如 "Wan (通义万相)"、"Seedance (即梦)"
- **t2v selection group** — 为 Seedance T2V 预留独立分组，不污染 I2V 列表
- **MuleRun key 配置 UI** — 全局设置 + 项目环境配置统一，含 3 步获取引导面板
- **MuleRun CLI 登录检测** — 当 CLI 已登录时显示 "✓ MuleRun CLI 已登录，无需手动填写"
- **R2V 模型全局默认设置** — 全局设置页新增 R2V 模型选择区

#### 变更
- **错误透传** — 资产生成失败显示 provider 真实错误信息（替代通用 "请检查 API 配置"），toast 支持复制错误详情
- **isVisibleModel 过滤 deprecated** — deprecated 状态的模型不再出现在 UI 下拉列表

#### 停用
- **wan2.6 全系列** — wan2.6-t2i、wan2.6-image、wan2.6-i2v、wan2.6-i2v-flash 等全部标记 deprecated，UI 不再显示

#### 修复
- **GPT-Image-2 size 兼容** — 自动转换 DashScope 格式 (1024*768) 为 GPT-Image-2 合法尺寸 (1536x1024)
- **GPT-Image-2 edit 参数** — `--images` (复数) 替代 `--image`
- **MuleRun CLI JSON 格式** — 兼容 string URL 数组和 object 数组两种返回格式
- **Pipeline 死代码** — 删除重复的 `create_asset_video_task` 定义，R2V-aware 版本生效
- **图片生成路由** — `AssetGenerator` 按 model_name 前缀路由到 MuleRouter adapter
- **R2V auto-switch 防护** — 直选 R2V 模型时跳过 I2V→R2V 自动切换
- **wan2.7-i2v 去掉残留 r2v capability** — 避免路由歧义
- **HappyHorse R2V 补 inputs.reference_images** — UI 正确显示参考图限制
- **MuleRouter submit 加 retry** — 提交任务与轮询/下载一致使用指数退避重试
