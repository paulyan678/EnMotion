# Packaging toolchain security baseline
pip==26.1.2
setuptools==83.0.0

# Web Framework
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# Multi-user server mode
SQLAlchemy==2.0.41
alembic==1.16.1
argon2-cffi==25.1.0
psycopg[binary]==3.2.9
celery==5.5.3
redis==6.4.0

# AI/ML Models
openai==2.46.0  # OpenAI-compatible client used by New API chat models

# Data Validation & Processing
pydantic==2.13.4
pydantic-settings==2.14.2

# Utilities
cryptography==48.0.1
python-dotenv==1.2.2
requests==2.34.2
pyyaml==6.0.3
PyJWT==2.13.0
pypdf==6.14.2
Pillow==12.3.0

# Optional Cloud Storage
oss2==2.19.1

# Audio Processing. Intel macOS stopped receiving current PyTorch wheels, so
# it uses the last compatible Python 3.12 pair.
torch==2.2.2 ; sys_platform == "darwin" and platform_machine == "x86_64"
torch==2.13.0 ; sys_platform != "darwin" or platform_machine != "x86_64"
demucs==4.1.0
soundfile==0.14.0

# Optional Dependencies
# Uncomment if needed:
# numpy>=1.24.0
# pillow>=10.0.0
